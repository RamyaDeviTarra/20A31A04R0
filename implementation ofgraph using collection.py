#implemantation of graph using collection of modules
#import diction for graph
from  collection import defaultdict
graph = defaultdict(list)
def addEdge(graph,u,v):
    graph[u].append(v)
def generate_edge(graph):
    edges = []
    for node in graph:
        for neighbour in graph[node]:
addEdge(graph,)
addEdge(graph,)
addEdge(graph,)
addEdge(graph,)
addEdge(graph,)
addEdge(graph,)
addEdge(graph,)
addEdge(graph,)

    
